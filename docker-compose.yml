version: '3'
services:
  app:
    build: .
    ports:
      - "1700:1700/udp"
    devices:
      - /dev/ttyACM0
    volumes:
      - /data:/lora_pkt_fwd/data
    network_mode: "bridge"
volumes:
  data:    
